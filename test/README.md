# Test plan

This directory intentionally contains no implementation tests yet. The first
coding change must begin with the core type-availability TDD slice in
`../specs/forge-ahead-triggers-extraction-spec.md`, then proceed through the
specified public seams and compiler regression fixture.
