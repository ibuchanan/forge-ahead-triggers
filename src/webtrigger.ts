/**
 * Forge web-trigger contracts and response utilities.
 *
 * The required `Record<string, string[]>` response-header invariant will be
 * implemented and protected by a compiler regression fixture.
 */
export {};
